class B implements AListener {
  public void doEvent(){
    //Что-то случилось в классе A
  }
}

//...
A a = new A();
B b = new B();
B c = new B();

a.addListener(b);// brand new comment 0
a.addListener(c);// brand new comment 0
a.addListener(b);//update in git_1
a.addListener(c);//update in git_1

a.doSomething(); //Оба объекта b и c выполняют doEvent